---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { 
  title = "VM MOBILE — Prodaja i servis fiskalnih kasa", 
  description = "Prodaja i servis fiskalnih kasa i potrošnog kancelarijskog materijala. Brz terenski rad u Petrovcu na Mlavi i celom Braničevskom okrugu. Serviser Mirko Vranić." 
} = Astro.props;
---

<!doctype html>
<html lang="sr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <script is:inline>
      if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        document.documentElement.classList.add('js');
      }
    </script>
  </head>
  <body>
    <a class="skip" href="#main">Preskoči na sadržaj</a>
    
    <header class="site-header">
      <a class="brand" href="#top" aria-label="VM MOBILE home">
        <img src="/logo.svg" alt="VM MOBILE Logo" class="brand-logo" />
      </a>
      <nav class="nav" aria-label="Primary navigation">
        <a href="#material">01 Usluge</a><a href="#work">02 Servis</a><a href="#office">03 Kancelarija</a><a href="#commission">04 Kontakt</a>
      </nav>
      <div class="place">
        <a href="tel:+381637495456" style="display: inline-flex; align-items: center; gap: 6px; font-size: 13px;">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-phone" style="display: inline-block;"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
          063 7 495 456
        </a>
      </div>
      <button class="menu-toggle" type="button" aria-expanded="false" aria-controls="mobile-menu">Meni [+]</button>
    </header>

    <nav class="mobile-menu" id="mobile-menu" aria-label="Mobile navigation">
      <a href="#material"><span>01</span>Usluge</a><a href="#work"><span>02</span>Servis</a><a href="#office"><span>03</span>Kancelarija</a><a href="#commission"><span>04</span>Kontakt</a>
    </nav>
    
    <aside class="folio" aria-hidden="true">
      <span class="folio-section">00 / POČETAK</span>
      <span class="folio-material">KASE / SERVIS / TEREN</span>
    </aside>

    <slot />

    <a href="#top" id="back-to-top" class="back-to-top" aria-label="Povratak na vrh">↑</a>

    <script>
    const init = () => {
      // Strike targets animation
      const strikeTargets = document.querySelectorAll([
        '.brand', '.nav a', '.hero-title span', '.hero-intro',
        '.eyebrow', '.manifest-title', '.works-title', '.office-title', '.project-title', '.commission-title'
      ].join(','));
      
      if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        strikeTargets.forEach(element => element.classList.add('voltage-strike'));
        window.setTimeout(() => {
          strikeTargets.forEach(element => element.classList.remove('voltage-strike'));
        }, 110);
      }

      // Mobile menu toggle
      const toggle = document.querySelector('.menu-toggle');
      const menu = document.querySelector('.mobile-menu');
      if (toggle && menu) {
        const menuLinks = menu.querySelectorAll('a');
        const closeMenu = () => {
          menu.classList.remove('open');
          document.body.classList.remove('menu-open');
          toggle.setAttribute('aria-expanded', 'false');
          toggle.textContent = 'Meni [+]';
        };
        toggle.addEventListener('click', () => {
          const open = !menu.classList.contains('open');
          menu.classList.toggle('open', open);
          document.body.classList.toggle('menu-open', open);
          toggle.setAttribute('aria-expanded', String(open));
          toggle.textContent = open ? 'Zatvori [×]' : 'Meni [+]';
        });
        menuLinks.forEach(link => link.addEventListener('click', closeMenu));
        document.addEventListener('keydown', event => { if (event.key === 'Escape') closeMenu(); });
      }

      // Folio Section Intersection Observer
      const folioSection = document.querySelector('.folio-section');
      const sections = document.querySelectorAll('[data-section]');
      if (folioSection && sections.length > 0) {
        const observer = new IntersectionObserver(entries => {
          const visible = entries.filter(entry => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
          if (visible && visible.target instanceof HTMLElement) {
            folioSection.textContent = visible.target.dataset.section || '';
          }
        }, { rootMargin: '-30% 0px -55% 0px', threshold: [0, .25, .5] });
        sections.forEach(section => observer.observe(section));
      }

      // Form validation
      const form = document.querySelector('#commission-form');
      const status = document.querySelector('#form-status');
      if (form && status) {
        form.addEventListener('submit', event => {
          event.preventDefault();
          if (!(form instanceof HTMLFormElement)) return;
          if (!form.checkValidity()) {
            status.textContent = 'Popunite obeležena polja pre slanja.';
            form.reportValidity();
            return;
          }
          status.textContent = 'Zahtev zabeležen. Povežite ovu formu sa backend-om pre lansiranja.';
          const submitSpan = form.querySelector('.submit span:first-child');
          if (submitSpan) submitSpan.textContent = 'Zabeleženo';
        });
      }

      // Pointer: direct tracking
      const finePointer = window.matchMedia('(hover: hover) and (pointer: fine)');
      if (finePointer.matches) {
        const cursor = document.createElement('div');
        cursor.className = 'ferrum-cursor';
        cursor.setAttribute('aria-hidden', 'true');
        document.body.append(cursor);
        document.body.classList.add('cursor-ready');

        let pointerX = -40;
        let pointerY = -40;
        let cursorFrame = 0;
        const drawCursor = () => {
          cursor.style.transform = `translate3d(${pointerX - 5}px, ${pointerY - 5}px, 0)`;
          cursorFrame = 0;
        };
        document.addEventListener('pointermove', event => {
          pointerX = event.clientX;
          pointerY = event.clientY;
          if (!cursorFrame) cursorFrame = requestAnimationFrame(drawCursor);
        }, { passive: true });
        
        document.addEventListener('pointerdown', () => cursor.classList.add('is-pressed'));
        document.addEventListener('pointerup', () => cursor.classList.remove('is-pressed'));
        
        document.querySelectorAll('figure').forEach(image => {
          image.addEventListener('pointerenter', () => cursor.classList.add('is-over-image'));
          image.addEventListener('pointerleave', () => cursor.classList.remove('is-over-image'));
        });
        
        document.querySelectorAll('a, button, input, select, textarea').forEach(action => {
          action.addEventListener('pointerenter', () => cursor.classList.add('is-over-action'));
          action.addEventListener('pointerleave', () => cursor.classList.remove('is-over-action'));
        });
        
        document.documentElement.addEventListener('mouseleave', () => cursor.style.display = 'none');
        document.documentElement.addEventListener('mouseenter', () => cursor.style.display = 'block');
      }

      // Images reveal without JS support
      const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      const revealFigures = [...document.querySelectorAll('figure')];
      revealFigures.forEach(figure => figure.classList.add('reveal'));
      document.body.classList.add('motion-ready');

      if (reducedMotion) {
        revealFigures.forEach(figure => figure.classList.add('is-revealed'));
      } else {
        const revealObserver = new IntersectionObserver(entries => {
          entries.forEach(entry => {
            if (!entry.isIntersecting) return;
            requestAnimationFrame(() => entry.target.classList.add('is-revealed'));
            revealObserver.unobserve(entry.target);
          });
        }, { root: null, rootMargin: '0px 0px -12% 0px', threshold: .08 });
        revealFigures.forEach(figure => revealObserver.observe(figure));
      }

      // Hero reveal control
      const hero = document.querySelector('.hero');
      const heroGrid = document.querySelector('.hero-grid');
      const heroDivider = document.querySelector('.hero-divider');
      const heroImage = document.querySelector('.hero-image');

      if (hero && heroGrid && heroDivider && heroImage) {
        let heroCut = window.innerWidth <= 800 ? 32 : 44;
        let draggingDivider = false;

        // Initialize slider cut at 0% so image is fully revealed initially
        const setHeroCut = (value: number) => {
          heroCut = Math.max(0, Math.min(100, value));
          const pct = `${heroCut}%`;
          (hero as HTMLElement).style.setProperty('--hero-cut', pct);
          (heroDivider as HTMLElement).style.left = pct;
          heroDivider.setAttribute('aria-valuenow', String(Math.round(heroCut)));
          heroDivider.setAttribute('aria-valuetext', `${Math.round(100 - heroCut)} procenata slike vidljivo`);
        };

        setHeroCut(0);

        if (reducedMotion) {
          setHeroCut(window.innerWidth <= 800 ? 32 : 44);
        } else {
          // Wait for first image reveal animation (180ms delay + 1350ms duration)
          setTimeout(() => {
            // Remove reveal classes so clip-path falls back to inline value (which is 0%)
            heroImage.classList.remove('reveal');
            heroImage.classList.remove('is-revealed');

            // Force reflow so the browser registers the state change without transitioning
            (heroImage as HTMLElement).offsetHeight;

            // Enable transitioning class
            hero.classList.add('transitioning');

            // Force reflow again to ensure transition styles are attached
            (heroImage as HTMLElement).offsetHeight;

            // Slide in black overlay/divider to default split point (using 1.2s transition)
            setHeroCut(window.innerWidth <= 800 ? 32 : 44);

            // Clean up transitioning class after transition completes (1200ms duration)
            setTimeout(() => {
              hero.classList.remove('transitioning');
            }, 1300);
          }, 1550);
        }

        const setCutFromPointer = (event: PointerEvent) => {
          const bounds = heroGrid.getBoundingClientRect();
          setHeroCut(((event.clientX - bounds.left) / bounds.width) * 100);
        };

        heroDivider.addEventListener('pointerdown', event => {
          draggingDivider = true;
          hero.classList.remove('transitioning'); // Remove transition immediately on user interaction
          heroDivider.setPointerCapture(event.pointerId);
          setCutFromPointer(event);
        });

        heroDivider.addEventListener('pointermove', event => {
          if (draggingDivider) setCutFromPointer(event);
        });

        const stopDivider = (event: PointerEvent) => {
          draggingDivider = false;
          if (heroDivider.hasPointerCapture(event.pointerId)) {
            heroDivider.releasePointerCapture(event.pointerId);
          }
        };

        heroDivider.addEventListener('pointerup', stopDivider);
        heroDivider.addEventListener('pointercancel', stopDivider);
        heroDivider.addEventListener('keydown', event => {
          if (event instanceof KeyboardEvent) {
            if (!['ArrowLeft', 'ArrowRight', 'Home', 'End'].includes(event.key)) return;
            event.preventDefault();
            if (event.key === 'ArrowLeft') setHeroCut(heroCut - 4);
            if (event.key === 'ArrowRight') setHeroCut(heroCut + 4);
            if (event.key === 'Home') setHeroCut(0);
            if (event.key === 'End') setHeroCut(100);
          }
        });

        setHeroCut(heroCut);
      }

      // Back to top behavior
      const backToTop = document.querySelector('#back-to-top');
      if (backToTop) {
        window.addEventListener('scroll', () => {
          if (window.scrollY > 400) {
            backToTop.classList.add('visible');
          } else {
            backToTop.classList.remove('visible');
          }
        }, { passive: true });
      }
    };

    if (document.readyState === 'complete') {
      init();
    } else {
      window.addEventListener('load', init);
    }
    </script>
  </body>
</html>
